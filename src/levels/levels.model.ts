// import { Pack, initialPacks } from "../pack/pack.model";

export interface Levels {
  currentLevel: number;
  // levelBoard: Pack[];
}

export const initialLavels: Levels = {
  currentLevel: 1,
  // levelBoard: initialPacks,
}
